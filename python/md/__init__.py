"""missile-defense — Python side (RL environment + training).

Placeholder package. In Step 2 this will expose a Gymnasium environment
wrapping the C++ core simulation (built as a nanobind extension module).
"""

__version__ = "0.1.0"
